# Match how the lockfile was produced: @firebase/rules-unit-testing (devDependency) peers on firebase@^9
# while the app uses firebase@^10, which a strict install rejects (ERESOLVE).
legacy-peer-deps=true
